Promesa = 
    Código que tiene varios estados. 
    Se mantiene en estado pendiente hasta que una de sus peticiones se cumpla.
    Se pueden ejecutar peticiones:
        * En caso que se haya resuelto la petición correctamente (.then())
        * Caso contrario que la petición no haya tendio éxito (.catch())